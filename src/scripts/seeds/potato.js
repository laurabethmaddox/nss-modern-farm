// Declare a variable named createPotato and export the 
// function
export const createPotato = () => {
    // Declare a variable named potato that holds the 
    // value of the objects type, height, and output properties
    const potato = {
        type: "Potato",
        height: 32,
        output: 2
    }
    // Return the value of the variable potato
    return potato
}