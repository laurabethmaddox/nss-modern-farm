// Declare a variable named createWheat and export the
// function 
export const createWheat = () => {
    // Declare a variable named wheat that holds the 
    // value of the objects type, height, and output properties
    const wheat = {
        type: "Wheat",
        height: 230,
        output: 6
    }
    // Return the value of the variable wheat
    return wheat
}