// Declare a variable named createSoybean and export the
// function
export const createSoybean = () => {
    // Declare a variable named soybean that holds the
    // value of objects type, height, and output properties
    const soybean = {
        type: "Soybean",
        height: 20,
        output: 4
    }
    // Return the value of the variable soybean
    return soybean
}